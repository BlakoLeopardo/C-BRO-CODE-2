#include <stdio.h>

int main(){
    // Constantes = valor fixo que não pode ser alterado pelo programa durante sua execução
    // Constantes devem ser escritas em letras maiusculas. 
const float PI = 3.14159;

    printf("%.2f", PI);

return 0;
}